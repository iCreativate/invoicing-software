# Deploy the Next.js app in client/ (not the root Express server).
# In Netlify UI: set "Base directory" to client if not using this file.
[build]
  base    = "client"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
