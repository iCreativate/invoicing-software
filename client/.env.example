# Backend API URL. For local dev, the default is http://localhost:5001.
# On Netlify (or any host), set this to your deployed backend URL so the frontend can connect.
# Example: https://your-api.railway.app
NEXT_PUBLIC_API_URL=http://localhost:5001
